services:
  todo-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL} # 从 .env 或环境变量注入 todo-backend-mongo-1
    ports:
      - "8080:80"
